# Translation API URLs
# 请将下面的 URL 替换为您自己的 DeepL API 地址，多个地址用英文逗号隔开
TRANSLATION_API_URLS=https://api.deeplx.org/xxxxxxxxxxx/translate,https://api.deeplx.org/yyyyyyyyyy/translate

# Server Configuration
PORT=8000
HOST=0.0.0.0
ENABLE_STREAMING=true
LOG_LEVEL=info

# Performance Tuning
# 以下值将覆盖 docker-compose.yml 中的默认值
MAX_REQUESTS_PER_MINUTE=240
TIMEOUT=30
MAX_WORKERS=8          # 此处使用您在.env中定义的值 8，而不是docker-compose.yml中的 5
CHECK_TIMEOUT=5        # 此处使用您在.env中定义的值 5，而不是docker-compose.yml中的 10

# URL Check Configuration
CHECK_INTERVAL=300
AUTO_UPDATE_URLS=true
MIN_AVAILABLE_URLS=2
RESULT_FORMAT=detailed

# Export Configuration
# 注意：此路径是容器内的绝对路径，并与 docker-compose.yml 中的 volumes 挂载相对应
EXPORT_PATH=/app/results/useful.txt

# Test Data Configuration
TEST_TEXT=Hello, world
TEST_SOURCE_LANG=EN
TEST_TARGET_LANG=ZH
